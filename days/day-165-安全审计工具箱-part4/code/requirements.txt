# 报告核心库只依赖标准库（hashlib / json / re / html / dataclasses），
# 所以这里**故意留空**——空依赖 = 零供应链风险。
#
# 保留这个文件的理由：Dockerfile 的构建层需要它来建立缓存边界，
# 将来真要加依赖（比如 jinja2 做模板渲染）时也有个明确落点。
#
# 如果将来要加依赖，请务必：
#   1) 固定精确版本（==x.y.z），不要用 >=；
#   2) 生成哈希锁文件（pip-compile --generate-hashes）；
#   3) 在 CI 里跑 pip-audit / safety 做漏洞检查。
