# Day 164 — 阶段项目（三）：安全审计工具箱 · 代理中间人 + 流量分析

> **Phase 10 — 网络安全开发（Day 146–165）** · 主题：安全审计工具箱（Day 162–165 四天项目的第 3 天）
>
> **使用边界（重要，读完再动手）**：
> - 只对**你自己拥有、或已获书面授权**的目标做审计。
> - 本课所有实验都跑在 `127.0.0.1` 上的**自建靶场**（`code/mitm_core.py` 里的
>   `LabOrigin` 实验网站 + `LabOpaqueTCP` 假 TLS 服务）。
> - 中间人代理**只监听回环地址**。代码里对 `0.0.0.0` 是**硬拒绝**：
>   一个"监听全网卡的开放 HTTP 代理"会在几分钟内被扫到并被滥用，
>   那是真实的法律责任，不是一个"配置选项"。
> - **不伪造证书、不降级 TLS、不破解隧道内容**。`CONNECT` 隧道一律如实记为
>   "不可见"，并计入覆盖缺口。
> - **不改包、不重放、不注入响应**。只做观测与记录。
> - 流量档案**默认脱敏**：口令 / 令牌 / Cookie 的**值**不落盘，
>   只留"字段名 + 值指纹（sha256 前 8 位）+ 长度"。报告能证明"明文传输了"，
>   **不能**被当成二次泄密的渠道。
> - 教材中的地址一律为回环或文档网段（RFC 5737 的 `192.0.2.0/24`）。

---

## 1. 学习目标

完成本课后，你应该能够：

- 说清阶段项目四天的分工，以及为什么"流量面"补上了前两天**结构性缺失**的那一块
- 解释**正向代理**与**反向代理**的区别，以及为什么审计要用正向代理
- 说清"绝对 URI 形式"与"源站形式"两种请求行的区别，以及为什么代理两种都要支持
- 解释 `CONNECT` 之后的字节流**为什么看不见**，以及"看不见"和"没问题"的区别
- 说清**逐跳头（hop-by-hop）**为什么必须在代理里剔除，不剔除会发生什么
- 解释代理**主动去掉 `Accept-Encoding`** 的收益与代价，以及生产环境为什么该换做法
- 说清**流模型（flow）**要记哪些字段，以及"记什么"背后的取舍
- 解释本课最重要的设计：**先脱敏、再检测**——检测逻辑为什么看不到明文也能工作
- 说清 **PII 擦除** 与 **PII 检测** 为什么必须分开，以及"擦掉就检测不到"这个坑怎么补
- 用 14 条规则把 flow 变成可读的 **Finding**（含证据 / 原因 / 修复建议）
- 说清**覆盖率**为什么必须独立统计，以及它如何把退出码推到 `4`
- 说清"优先级 = 严重度 × 置信度"为什么比"只看严重度"更诚实
- 明确本工具**不做**什么（解密、改包、重放、利用），以及这些边界为什么是必要的

---

## 2. 概念解释

### 2.1 阶段项目要做什么（今天站在哪个位置）

Day 159 做了"审计工具箱 v0"（规则引擎 + 报告 + CLI 门禁），Day 160 做了日志分析。
Day 162–165 把这几天分散的能力**拼成一个完整的资产审计工具**，四天分工：

```text
Day 162（前天）  资产面：端口扫描 + 目录爆破 + 指纹识别   ← 有哪些"资产"
Day 163（昨天）  漏洞面：SQLi 检测 + XSS 检测             ← 资产上"哪里可能有问题"
Day 164（今天）  流量面：代理中间人 + 流量分析            ← 实际"发生了什么"  ★本课
Day 165（明天）  交付面：报告生成 + Docker 部署           ← 最后"怎么交出去"
```

**为什么"流量面"不是"再补一层锦上添花"，而是补一块结构性缺失？**
因为前两天的结论**全部是推断**，它们各有各的盲区：

| 阶段 | 它回答的问题 | 它**回答不了**的问题 |
|---|---|---|
| Day 162 资产面 | 哪个端口开着、哪个路径返回 200 | 参数是怎么传的？走 GET 还是 POST？ |
| Day 163 漏洞面 | `id` 这个参数**疑似**可注入 | 真实业务流量里，凭据是明文还是密文？ |
| **Day 164 流量面** | **真实请求与响应的每一个字节** | （盲区：TLS 隧道内部） |

一句话概括三者的区别：

```text
资产面：门锁有没有装          → 快照
漏洞面：这把锁看起来像能撬      → 推断（有假阳性）
流量面：刚才有人拿钥匙开了门    → 事实（可复核、可留证）
```

**为什么"事实"这件事在审计里值钱？** 因为漏洞扫描器的输出**不可复核**：
"疑似布尔差分注入"这句话，开发会说"那是你探针的假阳性"。
而"登录口令 `password`（指纹 `a1b2c3d4`，11 字节）以明文出现在请求体里"
这句话**不需要争论**——它就是发生过的事实，而且开发一眼就知道要改哪一行。

**今天开工前，你手上应该已经有两份东西**（由 Day 162/163 产出）：

1. **URL + 参数清单** → 代理要用它来决定"该代理哪些流量"；
2. **一场真实的业务会话**（登录、查询、下单…）→ 代理要用它来做**基线**。

### 2.2 中间人代理：什么叫"中间人"，什么叫"代理"

先用一句话把三个名词钉住：

| 名词 | 一句话定义 | 谁在监听 | 典型用途 |
|---|---|---|---|
| **正向代理** | 代表**客户端**去访问服务端 | **客户端侧** | 上网代理、审计、抓包 |
| **反向代理** | 代表**服务端**来接收客户端 | **服务端侧** | Nginx 负载均衡、WAF |
| **中间人（MITM）** | 站在两端之间，**两段连接都是它** | 中间任意位置 | 审计、调试、劫持（看你有没有授权） |

**为什么审计要用正向代理，而不是反向代理？**
因为审计的立场是"**我代表客户端，我要看客户端发了什么**"。
反向代理给你的是服务端视角——它看到的是"所有客户端"，但你**装不到**它上面，
除非你本来就拥有那台服务器（那就不叫审计了，叫自测）。
正向代理的好处是：**你在自己的机器上就能起一个**，然后把客户端的流量指过去。

**"中间人"这个名字为什么让人紧张？** 因为它字面就是攻击手法。
同样的代码，装在**你自己的**机器上叫"内网审计"，装在**别人的**机器上叫"中间人攻击"。
区别**不在技术，在授权**。这就是本课把"范围门禁"放在第一个提交、
把"只监听回环"做成硬断言的原因：**把伦理约束写进代码，而不是写进注释**。

#### 两段连接，两种角色

```text
   ┌──────────┐   ①客户端→代理    ┌────────────┐   ②代理→服务端   ┌──────────┐
   │  客户端   │  GET http://…     │  正向代理   │  GET /path       │  服务端   │
   │ (审计会话)│ ────────────────▶ │ (本课实现)  │ ───────────────▶ │ (靶场)   │
   │          │ ◀──────────────── │            │ ◀─────────────── │          │
   └──────────┘   ④响应回客户端     └────────────┘   ③响应回代理     └──────────┘
                                         │
                                         ▼
                                  flows.jsonl（流量档案）
```

**注意这里的"两次解析"**：代理必须把 ① 的报文**完整收下来**（包括请求体），
再用 ② 的形式**重新发出**。它不是一根管子，而是一个**会思考的中转站**。
"管子"（隧道）是 `CONNECT` 才做的事——那才是它唯一"不思考"的时刻（见 2.5）。

### 2.3 为什么"流量面"是观测，而前两面是推断

用一个具体的例子来看三层的差异。假设目标有个 `/login`：

```text
Day 162 看：/login 返回 200                        → "这个路径存在"
Day 163 看：/login?id=1 与 /login?id=1' 行为不同    → "id 可能可注入"（假阳性风险高）
Day 164 看：POST /login 明文body里 password=xxx     → "口令以明文传输"（事实）
         看：响应 Set-Cookie 缺 HttpOnly、Secure    → "会话令牌可被 JS 读取"（事实）
```

**为什么流量面能拿到"事实"？** 因为它不需要**制造**任何输入。
它只是把**已经发生的**业务对话录下来。
这一点带来两个重要性质：

1. **零打扰**：不对业务系统注入任何探针，不增加负载（只增加一次转发）。
   所以它**可以用在生产环境**——这是漏洞扫描器做不到的。
2. **可复核**：报告里的每一条都能点回到具体某一条 flow 的**原始字段**
   （值是脱敏的，但结构、长度、指纹都在）。

**代价也很明确**：

- **被动**：用户没点过的路径，流量面永远看不到（所以要靠 Day 162 的目录爆破补面）；
- **盲区**：TLS 隧道内部的内容它看不见（见 2.5）；
- **依赖场景**：需要有人（或脚本）真的去跑一遍业务，否则档案里啥也没有。

**所以三层的正确关系是"互相补盲"，不是"谁替代谁"**：

```text
资产面 给流量面：该盯哪些 URL
漏洞面 给流量面：哪条推测值得在流量里找证据
流量面 给漏洞面：这条推测到底是不是真的发生了
```

### 2.4 代理协议的细节：两种请求行形式

这是手写代理**第一个会踩的坑**，必须讲清楚。

HTTP 请求行的格式是：

```text
METHOD  request-target  HTTP-VERSION
```

而 `request-target` 在代理场景下有两种形式：

```http
① 绝对 URI 形式（绝对形式）—— 正向代理的"标准姿势"
GET http://127.0.0.1:18081/api/user?id=1 HTTP/1.1
Host: 127.0.0.1:18081

② 源站形式（origin-form）—— 直接对源站说话时用
GET /api/user?id=1 HTTP/1.1
Host: 127.0.0.1:18081
```

**为什么会有两种？** 因为"收到报文的人"不同：

| 收报文的人 | 它知道"目标是谁"吗 | 所以 request-target 是 |
|---|---|---|
| 源站服务器 | 知道（就是它自己） | 源站形式（只需要路径） |
| 正向代理 | **不知道** | 绝对 URI 形式（必须自带主机名） |

**为什么代理两种都得支持？** 因为：

- 标准客户端（浏览器、`requests`、`urllib`）配了代理后发的是 ①；
- 但有些库/工具在"把代理当网关"或用某些 HTTP/1.0 客户端时，会发 ②；
- 更麻烦的是：**有些客户端发 ①，却想让你只转 ② 的部分**（比如它对代理说
  "帮我连到 A"，但 `Host` 头写的是 B —— 这叫 **Host 头投毒**，
  是缓存的经典攻击面）。

本课的 `_resolve()` 两种都处理，并且在**越界检查**时用的是"request-target 里的主机"，
而不是 `Host` 头——**这是一个有意识的安全选择**：

> 如果门禁信任 `Host` 头，攻击者就能用 `GET http://evil.com/ HTTP/1.1` +
> `Host: 127.0.0.1` 让门禁以为"这是回环"，实际却连去了外部。
> **门禁必须校验"真正会连过去的主机"，而不是"对方声称的主机"。**

### 2.5 `CONNECT`：为什么隧道里的东西看不见

HTTPS 流量经过代理时，客户端不会把明文请求给代理（那 HTTPS 就没意义了）。
它会先让代理**开一条裸字节隧道**：

```text
客户端                   代理                     服务端:443
  │  CONNECT example.com:443 HTTP/1.1  │                │
  │ ──────────────────────────────────▶ │                │
  │                                     │  TCP connect   │
  │                                     │ ──────────────▶│
  │  HTTP/1.1 200 Connection Established │                │
  │ ◀────────────────────────────────── │                │
  │                                     │                │
  │ ══════ TLS 握手（代理看不懂）══════════════════════▶  │
  │ ══════ 加密的 HTTP 请求（代理看不懂）════════════════▶  │
```

**代理在这一刻变成了"哑管子"**：它能看到的有三样（而且只有三样）：

1. 目标主机名与端口（`CONNECT example.com:443`）——**这是能看出"去了哪"的**；
2. 双方各传了多少字节、持续多久；
3. 什么时候关闭。

**它看不到的**：URL 路径、请求头、请求体、响应体——**全部**。

**为什么不"顺手解密一下"？** 因为 TLS 的信任模型不是"谁转发谁能看"：

```text
证书信任链（简化）
  浏览器/客户端
     └─ 信任 ─▶ 一串内置根 CA
                   └─ 签发 ─▶ example.com 的证书（含公钥）
                                └─ 只有持有对应私钥的一方，才能完成握手
```

要解密，只有两条路，**都没有捷径**：

| 做法 | 前提 | 性质 |
|---|---|---|
| 拿到**目标私钥** | 你拥有那个服务器 | 合法，但说明你不需要"中间人" |
| 让客户端**信任你的自签 CA** | 你在客户端装了根证书（企业 MDM） | 在**自己拥有**的设备上合法；在别人设备上就是劫持 |

**所以本课的选择是**：不尝试、也不假装能解密，而是把"看不见"**如实记成一条覆盖缺口**，
最终把退出码推到 `4`。这比"报告里啥也没写，看起来一切正常"诚实得多。

> **这条规则在报告里的措辞极其重要**：
> `CONNECT` 的 flow 会让报告写"**本段流量未被检查**"，
> 而**不会**写"未发现问题"。因为"没看"和"没问题"是两件事——
> 把它们混为一谈，是整个安全行业最常见、也最危险的一种自欺。

### 2.6 流模型：一次往返要记哪些字段

**什么是"流"（flow）？** 一次完整的"请求 → 响应"往返。
它是流量面的**最小证据单元**：报告里的每一条结论都能点回到某个 `fid`。

**为什么用"流"而不是"包"？** 因为：

| 粒度 | 得到什么 | 适不适合做审计报告 |
|---|---|---|
| 包（packet） | 字节级真相，但一条请求可能拆成几十个包 | ❌ 太细，规则引擎要自己重组 TCP |
| **流（我方定义）** | 语义级：方法、URL、头、体、状态、耗时 | ✅ 一条结论对应一条流 |
| 会话（session） | 一次登录到退出的全过程 | 需要额外定义"会话边界"，复杂 |

（Day 157 用 `pyshark` 做的是"包"层；今天做的是"应用层语义"层。两者互补。）

本课记的字段，以及**为什么记它/为什么脱敏它**：

| 字段 | 记它的理由 | 处理方式 |
|---|---|---|
| `fid` | 让每条结论可点回原始证据 | 自增编号 |
| `ts` / `duration_ms` | 时间线、慢响应、性能基线 | 原样 |
| `method` / `scheme` / `host` / `port` | 判断"明文还是密文"、"是不是写方法" | 原样 |
| `path` / `url` | 定位是哪个端点出的问题 | **查询串脱敏** |
| `query_keys` | 单独留下"哪些参数名是敏感的" | 只留参数名 |
| `request_headers` | 鉴权方式、内容类型、UA | 凭据类头整条脱敏 |
| `request_body` | 判定"凭据是否明文传输" | **字段值脱敏** |
| `response_status` | 成功/失败/重定向/服务端错误 | 原样 |
| `response_headers` | `Set-Cookie` 标记、安全头、指纹外泄 | `Set-Cookie` 等脱敏 |
| `response_body` | 报错回显、响应体泄密 | **字段值脱敏 + PII 擦除 + 截断** |
| `inspected` | **这条流到底看没看见内容** | 布尔值（隧道 = `false`） |
| `error` | 传输层失败也是事实，不能静默 | 异常类型 + 消息 |
| `note` | 记录"命中了哪些敏感字段" | 只写**字段名**，不写值 |

**两个字段值得单独解释：**

**① 为什么 `inspected` 要显式存成一个字段，而不是靠"body 是空的"推断？**
因为"body 为空"有太多可能：`204 No Content`、真的返回空、
被截断、隧道不可见、请求失败——**这一堆完全不同的处境，会塌缩成同一个表象**。
显式存 `inspected` 就是拒绝这种塌缩。这是一个通用原则：

> **当多种原因会导致同一种表象时，必须把"原因"显式存下来，
> 否则下游只能在"表象"上做猜测。**

**② 为什么 `note` 只写字段名？** 因为报告需要的是
"这条流里出现了 `password` 字段"这个**结论**，
而不是"`password` 的值是什么"这个**秘密**。少写一个字节都不行的那种克制。

### 2.7 本课最重要的设计：先脱敏，再检测

这是 Day 164 唯一一个"你必须能讲清楚，否则不算学会"的设计。

**先看一个反面设计**（大多数人第一版都会这么写）：

```python
# ❌ 反面写法：先检测明文，再把明文删掉
if re.search(r"password=([^&]+)", body):
    findings.append("明文口令传输")
    body = re.sub(r"password=([^&]+)", "password=<redacted>", body)   # 事后补救
```

**它的三个问题**：

| 问题 | 后果 |
|---|---|
| 检测依赖**明文** | 任何加密/编码变形（Base64、URL 编码）都会让检测失效 |
| 明文**先进了内存** | 它可能落进临时文件、日志、异常栈、core dump |
| "先检测后清理"是**两段逻辑** | 一旦漏掉一处（比如 `path` 里的同一个值），就全线失守 |

**本课的做法：值在**进入档案对象之前**就已经被换成了指纹。**

```python
# ✅ 本课写法：redact 是第一道工序，不是补救措施
req_body_redacted, body_hits = redact_body(req_body_text, ctype)   # 值 → <redacted:hash/NB>
req_body_redacted, req_pii   = mask_pii(req_body_redacted)         # PII → <pii:类别>
flow = Flow(..., request_body=req_body_redacted, note=body_hits)
```

于是检测逻辑面对的是这样的文本：

```text
username=ada&password=%3Credacted%3A9f8e7d6c%2F11B%3E
```

**它怎么还能判定"这是明文口令"？** 靠两个**结构信号**：

1. **字段名**：`password` 出现在里面 → 说明这个字段承载的是口令；
2. **值的指纹标记** `redacted:` → 说明这个值**确实存在且被脱敏了**
   （不是空值、不是占位符）。

再加上传输层信号（`scheme == "http"`），结论就成立：**"明文传输了口令"**。

**这个设计的收益是三层叠加的：**

```text
①  档案里没有明文         → 审计工具自己不会成为泄密渠道（合规底线）
②  检测不依赖明文         → 连"我要看一看才知道"这一步都不需要（更安全）
③  脱敏是单一入口         → 只要 redact 覆盖面完整，就不会有漏网之鱼
```

**代价（必须承认）：**

- **可读性下降**：看报告时要对照字段名读，不能一眼看到原文；
- **指纹可被暴力破解**：如果值很短（比如 4 位 PIN），
  `sha256 前 8 位` 是可以被离线穷举的。所以指纹的作用是"**比对**"，
  不是"**保密**"——真正的保密靠"值本身没被记下来"。

**这条"先脱敏再检测"的原则，可以推广到所有日志系统**：

> 日志/档案的**默认状态**应当是"不含秘密"，
> 需要秘密的场景必须**显式申请**（而不是默认记下来、事后删）。

### 2.8 覆盖率：为什么"看不见"必须独立成一种结果

上一节的 `inspected` 字段，最终会汇成一个整体指标：**覆盖率**
= 已检查的 flow 数 ÷ 总 flow 数。

**为什么它必须单独统计、单独汇报、单独进退出码？** 用一个反例说明：

```text
假设一次审计：100 个请求，其中 60 个走 HTTPS（CONNECT 隧道，看不见）
                只有 40 个走 HTTP（看见了）

如果报告只写"未发现问题"：
    读者以为：100 个都检查过了，安全 ✅
    实际是：  只有 40 个检查过，另外 60 个是黑洞 ❌
```

**这是审计报告里最危险的一句话**——它把"我没看"伪装成"没问题"。
本课的对策是把这件事**结构性地**表达出来：

```text
报告的"发现汇总"一节开头就写：
  （本规则集未命中任何发现。注意：这不等于『安全』，见覆盖率一节。）

覆盖率一节单独列出：
  总 flow / 已检查 / 未检查（隧道）/ 传输错误 / 覆盖率%

退出码：
  有隧道或传输错误 → 4（结论不可信），优先级高于 3（有发现）
```

**为什么退出码 `4` 的优先级高于 `3`（有发现）？**
因为"没测完"会**掩盖**发现：如果一半流量是黑洞，
报告里即使有 P0 也是"不完整的 P0"——先告诉上层"这轮结论不可信"，
再谈发现了什么，是更诚实的顺序。
（越界的 `2` 由异常路径抛出，语义是"根本没开始测"，自然排在 `4` 之前。）

**最后一点：传输错误（连不上、超时）也算覆盖缺口。**
因为"连不上"和"没问题"在报告里长得一样——都是"没有发现"。
不显式区分，报告就在撒谎。

---
