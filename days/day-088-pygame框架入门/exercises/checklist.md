# Day 088 - 完成清单

## 今日学习目标

- [ ] 理解 Pygame 框架的核心模块和功能
- [ ] 掌握游戏循环的基本结构（事件→更新→渲染）
- [ ] 能够创建 Pygame 窗口并处理用户输入
- [ ] 理解精灵系统的概念和使用方法
- [ ] 掌握基本的碰撞检测方法

## 代码练习

- [ ] 运行 `01-pygame窗口基础.py`，理解窗口创建流程
- [ ] 运行 `02-精灵与碰撞检测.py`，观察精灵移动和碰撞
- [ ] 运行 `03-弹跳小球实战.py`，体验完整的小游戏
- [ ] 尝试修改代码中的颜色、速度等参数

## 理解检查

- [ ] 能解释游戏循环的三个阶段分别做什么
- [ ] 能区分 `event.get()` 和 `key.get_pressed()` 的使用场景
- [ ] 能解释为什么 Pygame 的 Y 轴是向下增长的
- [ ] 能说明 `fill()` + `flip()` 为什么需要配合使用

---

## 练习题

### 基础题

**1. 窗口自定义**
> 修改 `01-pygame窗口基础.py`，将窗口大小改为 1024×768，标题改为你的名字。

**2. 形状绘制**
> 在 `01-pygame窗口基础.py` 的基础上，添加以下图形：
> - 一个黄色的等边三角形（提示：用 `pygame.draw.polygon()`）
> - 一个空心的蓝色圆环（提示：先画大圆，再画小圆填充背景色）

**3. 颜色渐变**
> 修改 `01-pygame窗口基础.py`，让背景颜色随时间变化（R/G/B 值随帧数循环变化）。

### 进阶题

**4. 追踪鼠标**
> 创建一个新程序，让一个小方块始终跟随鼠标移动。提示：
> ```python
> mouse_x, mouse_y = pygame.mouse.get_pos()
> ```

**5. 弹跳球增强版**
> 在 `03-弹跳小球实战.py` 的基础上，添加以下功能：
> - 按 "+" 键加速所有小球
> - 按 "-" 键减速所有小球
> - 按 "C" 键清空所有小球
> - 显示实际 FPS（帧率）
